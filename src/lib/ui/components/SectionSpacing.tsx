import { Box } from "@chakra-ui/react";

// The spacing between each section of a page
export const SectionSpacing = () => {
  return (
    <Box
      height={{
        base: "100px",
        md: "200px",
      }}
    />
  );
};
